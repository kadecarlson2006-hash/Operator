<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only: the development backend usually runs as plain HTTP on the LAN
  (http://10.0.2.2:8080 from the emulator, http://<laptop-ip>:8080 from a phone).
  Release builds keep Android's default, which refuses cleartext.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:usesCleartextTraffic="true"
        tools:targetApi="28" />
</manifest>
